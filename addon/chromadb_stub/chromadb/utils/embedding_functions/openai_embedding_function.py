# chromadb.utils.embedding_functions.openai_embedding_function stub
# crewai imports OpenAIEmbeddingFunction from this module at startup.


class OpenAIEmbeddingFunction:
    """Stub — chromadb not installed. Memory/RAG features disabled."""
    def __init__(self, *args, **kwargs):
        pass

    def __call__(self, input):
        raise NotImplementedError(
            "chromadb is not installed. Memory/RAG features are disabled in this add-on."
        )
